import * as React from "react";
import Svg, { Rect, type SvgProps } from "react-native-svg";

const SvgB031 = (props: SvgProps) => (
	<Svg
		width="1em"
		height="1em"
		fill="none"
		viewBox="0 0 24 24"
		role="img"
		{...props}
	>
		<Rect x={6} y={3} width={12} height={18} rx={2} fill="currentColor" />
	</Svg>
);

export default SvgB031;
